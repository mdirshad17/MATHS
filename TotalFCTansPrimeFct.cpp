#include<bits/stdc++.h>
using namespace std;
#define MAXN 1e6+1;
vector<int> sieve()
{
	vector<int> res(MAXN, 0);
	for (int i = 0; i < MAXN; i++)
	{
		res[i]
	}
}
int main()
{
	int q;
	cin >> q;
	while (q--)
	{

	}
}